// Canonical definitions live in ../types.ts to avoid circular imports.
// This file re-exports them for consumers who import from the styles path.
export type { CellStyleRule, RowStyleRule } from '../types.js';

